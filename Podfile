# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HelpCare' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HelpCare
  pod 'Alamofire'
  pod 'BMSCore', '~> 2.0'
  pod 'BMSPush', '~> 3.0'
  pod 'WSTagsField'
  
  target 'HelpCareTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HelpCareUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
